{% extends 'base.html.twig' %}

{% block title %}Password zurücksetzten{% endblock %}

{% block body %}
<div class="container" style="margin-top: 100px">
    <h1>Password zurücksetzten</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Password zurücksetzten</button>
    {{ form_end(resetForm) }}
</div>
{% endblock %}
