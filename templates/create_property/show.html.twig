{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <div class="container">
        <h1>{{ property.propertyTitle}}</h1>
        <img src="{{ asset('bilder/' ~ property.bild) }}">
    </div>
{% endblock %}
